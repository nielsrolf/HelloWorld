name: "Greet World"

on: push
#   schedule:
#     - cron: "14 22 * * *"

jobs:
  greet:
    name: greet
    runs-on: ubuntu-latest

    steps:
      - run: date
    # - name: Checkout
    #   uses: actions/checkout@v2
    # - run: date
    # - run: source greet_slowly.sh 
    #   env:
    #     SSH_KEY: ${{ secrets.SSH_KEY }}
